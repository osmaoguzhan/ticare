import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#4e72df",
      text: "#00000099",
      menuText: "white",
      buttonColor: "#f8f8f8e6",
      success: "#008000",
      error: "#ff0000",
      white: "#ffffff",
    },
  },
});
